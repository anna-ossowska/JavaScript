// In the following code, import the default export from the export_defaukt.js file, found in the same directory as this file.
// Give the import the name subtract.

import subtract from './export_default.js';

subtract(10, 8);